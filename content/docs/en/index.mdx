---
title: AI Router Documentation
description: One API key and one base URL to reach many model providers through a unified, OpenAI-compatible gateway.
---

import {
  Server,
  Network,
  Code2,
  Headphones,
  Bot,
  Handshake,
  Container,
  Boxes,
  Rocket,
} from 'lucide-react';

## Welcome to AI Router

AI Router is a unified, OpenAI-compatible AI gateway. Instead of managing a separate key, SDK, and base URL for every upstream provider, you get a **single API key** and a **single base URL** that route requests to many models through one consistent interface. Organize models into model groups, control access with token groups, and track quotas and usage from one place.

<Callout type="info">
AI Router is built on a popular open-source AI gateway and customized as its own product. The source is available on [GitHub](https://github.com/ssgzy/Router-new-api).
</Callout>

## Quick Start

Point any OpenAI-compatible client at the gateway by setting two values: the base URL and your AI Router API key.

```bash
curl https://ai.sammier.com/v1/chat/completions \
  -H "Authorization: Bearer $CODE_ROUTER_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "your-model-name",
    "messages": [{"role": "user", "content": "Hello!"}]
  }'
```

The OpenAI-compatible endpoint is `https://ai.sammier.com/v1`. Most SDKs only require overriding the base URL — for example, set `base_url` in the OpenAI Python SDK or `baseURL` in the Node SDK.

<Callout type="warn">
Replace `https://ai.sammier.com` with the base URL of your own deployment, and use a model name that exists in your configured model groups.
</Callout>

## Browse the Documentation

Explore guides and references to get the most out of AI Router.

<Cards>
  <Card
    icon={<Code2 />}
    title="API Reference"
    description="OpenAI-compatible endpoints, request and response formats, and call examples."
    href="/en/docs/api"
  />
  <Card
    icon={<Bot />}
    title="App Integrations"
    description="Connect AI Router to chat clients, IDEs, and other tools that speak the OpenAI protocol."
    href="/en/docs/apps"
  />
  <Card
    icon={<Headphones />}
    title="Help & Support"
    description="Frequently asked questions and where to ask for help."
    href="/en/docs/support"
  />
  <Card
    icon={<Handshake />}
    title="Business"
    description="Partnership and collaboration information. (Details TBD.)"
    href="/en/docs/business"
  />
</Cards>
